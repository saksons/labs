global_defs {
}

vrrp_instance VI_1 {
state BACKUP
interface enp0s8 #введите имя интерфейса. [чтобы увидеть имя интерфейса: $ ip a ]
virtual_router_id 51
priority 101 # 101 для мастера. 100 для резервного копирования. [приоритет мастера> приоритет резервного копирования]
advert_int 1
authentication {
auth_type PASS
auth_pass 1111 #пароль
}
virtual_ipaddress {
 {{ vrp }} # используйте виртуальный ip-адрес.
}
}